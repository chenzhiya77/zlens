# T08 — 端到端验收与真实库对账

**Blocked by:** T06 — 日趋势图 + 模型排行视图; T07 — 项目分布 / 性能分位数 / 健康度三视图
**Status:** ready-for-agent

## User story

作为工具的唯一用户,我要确认页面上每一个数字都和既有的验证脚本口径一致,然后才把 v1 当作可信的日常工具使用。

## What to build

收尾验收票:对真实数据库的 live 冒烟测试(meta 可用、记录数大于零、各端点 200);六视图数值与既有 token 统计脚本的人工/自动对账;真实场景下的只读无干扰复验(ZCode 会话进行中查询);文档同步(README 快速开始、AGENTS.md 地图核对、CHANGELOG 补充 v1 条目)。发现口径偏差时修数据层并在本票内回归。

## Acceptance criteria

- [x] live 标记冒烟测试通过且默认离线套件仍排除它(2026-08-26:1 passed / 25 deselected)
- [x] 总览五类 token 合计与独立统计脚本输出一致(同库同刻对账:七项指标 API 与直连 SQL 逐项相等,
      1,071 次请求 / 166,137,153 total tokens)
- [x] ZCode 对话进行中连续查询,对话无感知、查询无阻塞(20 连发全 200;整个 v1 开发过程本身
      即为活跃对话下的持续验证)
- [x] 价格表改动 → 页面金额变化($184.66 → 改价后 $289.42,热生效);删除价格表 → 全部模型
      转为"未计价"、总额回退 null
- [x] README / AGENTS.md / CHANGELOG 与实际行为一致;`make check`(lint + 25 测试 + 前端构建)全绿
