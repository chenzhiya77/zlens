# T30 — [已退役] 前端积分维度(积分列 / 价格表积分单价 / 买断指引)

**Status:** retired (2026-09-05) —— 拆入 **T35**(积分单价录入)与 **T36**(前端积分视图)

本票起草于 v3 取证之前。原设计中的有效内容已并入新票:

- 积分列「— vs 未上报」渲染纪律 → T36(升级为 `tokens_reported=false` 数据驱动的
  「未提供」标记,不按 source id 硬编码);
- 价格表 ¥/积分单价录入 → T35(两段键 `source|basis`,schema 由 T31 落地);
- 买断指引(Qoder 订阅费记 `buyout_amount`)仍有效,随 T35 的文档同步一并写入
  AGENTS.md;
- 附录的五档模型参考单价(Qwen3.8-Max ≈0.0102 等)已并入 research §九的逐通道费率表。

不要按本票开工。
