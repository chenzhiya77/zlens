# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- 运行质量页按画布重排 + 来源维度(T26):页内自绘页头(累计口径副标题 + 「自动刷新 · 5 分钟」,
  真实 `refetchInterval` 与文案一致),其下健康结论栏(状态点按出错率 <5% 绿 / <15% amber /
  ≥15% 红,与分源对比卡行级共用阈值)、单一强调色的 P50/P90/P99 条(按本卡 P99 归一)、
  三张统计卡、错误类型分布卡。**来源 chips**(全部 / zcode / minimax / opencode)切换时
  performance / health 请求带 `?source=` 由后端重算整页,`source` 进 URL 可分享;**分源对比卡**
  恒驻不受 chips 影响——对每个可用源并行发独立请求,前端只做纯计数除法,P99 直接用后端
  `latency_stats`,不做任何本地拆分;零值比率变暗,非零错误率 ≥5% 上红;仅一个可用源时
  整卡隐藏;小样本口径注固定展示。真实三源冒烟对账:对比卡三行请求数之和 = 健康结论栏总数。
  画布里时间范围 seg 因 `/api/performance`、`/api/health` 尚无窗口参数而**暂不渲染**,加窗口另开票。
- 总览页交互接线(T24,阶段 B 收尾):周期选择器(当天 / 近 7/30/90 天 + 全部 + 自定义区间,
  前端把相对窗口翻译成绝对日期)与来源 chips(T18 的 `meta.sources`,不可用来源置灰并
  带原因)切换时**所有卡片、趋势图、表格由后端按参数重算**;搜索框是唯一的客户端过滤
  (只筛显示行);7 个数值列表头可点排序(默认成本降序,后端 T19 扩展到全 7 列,未计价
  行恒排最后,表头带升降指示器);合计行读 `totals`,成本未计价显示「未计价」;导出按钮
  直链 `/api/export` 下载带日期文件名的 Markdown;总览查询每小时自动刷新,页脚文案与
  一致。`sort` / `order` / `source` / 窗口全部进 URL query,刷新/前进后退/分享链接还原
  同一视图。Hero 右侧新增同窗口成本小图:定长窗口按日折线、「全部」按月柱状,未计价的
  日子断口不补 0(`connectNulls: false`);环比红涨绿跌(成本涨=红),delta 为 null 处
  一律不渲染;缓存命中率带「按 token 计」说明。
- 表格合计行数据(T20):`/api/overview` 新增 `totals`(请求数 + 六个 token 档 + 成本),
  由后端按**窗口 + 来源过滤后的全量**计算,前端不再自己把 `by_model` 加一遍。诚实规则
  沿用:任一模型未计价 → `totals.estimated_cost` 为 null,token 各档照常求和。
  「全部合计 · 共 N 个模型」的渲染在 T24(修正设计稿「本页合计 · 前 5 个模型」的自相矛盾,
  不做分页)。
- Markdown 导出(T22):新增 `GET /api/export`,固定输出按模型明细的 Markdown 表格
  (不做 csv、不留 `format` 参数),`start` / `end` / `source` / `sort` / `order` 全部生效,
  响应头带 `Content-Disposition` 文件名(如 `zlens-models-2026-08-29.md`)。三条硬规矩:
  token 列只导**原始整数**(「5.49 亿」没法二次计算)、成本列不带货币符号、null 写
  **「未计价」**绝不写 0;文件头自带口径说明(四档互斥、成本为估算、未计价含义、生成时间、
  别名不包含)——这份数据离开应用后就没有上下文了。前端下载按钮在 T24。
- 总览环比(T16):`/api/overview` 新增 `delta`,对**等长前移**的上期窗口(选 30 天就比
  紧挨着的前 30 天,不做自然月对齐——天数不同比总量会得出相反结论)给出 `request_count`
  与 `estimated_cost` 的 `previous` 与 `change_rate`。三条诚实护栏:上期窗口不完整(起点早于
  首条数据)或完全无请求 → `delta` 整体为 null,绝不显示"+300%"式的假环比;任一侧成本
  未计价 → 该项 `change_rate` 为 null 但 `previous` 照常给出;previous 为 0 → `change_rate`
  为 null 而非 Infinity。「全部」或半开窗口无环比。前端红涨绿跌渲染在 T24。
- 趋势月度聚合(T17):`/api/trends/daily` 新增 `granularity=day|month`(缺省 day,行为不变,
  回显字段 `granularity` 便于前端确认)。月度与日度**共用同一段未计价判定**(月内任一天
  未计价 → 该月成本 null),token 各档两种粒度求和一致;没有数据的月份不补 0。「全部」
  周期走按月柱状图的渲染在 T24。
- 缓存命中率(T21):`/api/overview` 总计与每个模型行新增 `cache_hit_rate`,公式为
  **缓存读 ÷(输入 + 缓存读 + 缓存写)**(方案 A,分母 = 全部 prompt token,与四档互斥契约
  天然对齐);分母为 0 时是 `null` 而不是 0% 或 100%。当前三源缓存写恒 0,该选择零代价,
  纯为将来买保险——一旦有模型开始写缓存,B 方案会算出「命中率 100% 但仍在按最贵档付费」
  的荒谬数。KPI 卡上的百分比渲染与「按 token 计」说明在 T24。
- zcode 逐行 clamp 测试补全(T25,只补测试不改逻辑):锁死「一行 `cache_read > input` 的
  脏数据只让该行贡献 0、不能抵消其它正常行」的行为,并用反向断言证明测试有约束力——
  把 SQL 里的 `MAX` 挪到 `SUM` 外在这组数据上会得到不同结果。
- 表格排序参数(T19):`/api/overview` 与 `/api/models` 新增 `sort`
  (`total_tokens`|`estimated_cost`|`request_count`,缺省 `total_tokens` 保持既有顺序)与
  `order`(缺省 `desc`),非法值 422 并列出可选列。**未计价行恒排最后,与升降序无关**——
  「不知道」不能排在中间读成「便宜」,这条规则在后端 `sort_usage_rows` 里执行,前端只传参。
  表头点击与 URL 持久化的 UI 在 T24(届时总览默认 `sort=estimated_cost&order=desc`,
  对齐设计稿副标题「按成本降序」)。
- 来源枚举(T18):`/api/meta` 新增 `sources` 数组(`{id, available, error}`),列出**全部已注册**
  来源——不可用的也在(置灰并携带原因),而不是一挂就从界面消失让人以为从没用过;
  `source_id` 字段原样保留,与 `?source=`、日期窗口参数组合生效。前端来源 chips 的 UI 在 T24。
- 日期窗口参数下推(T15):`/api/overview`、`/api/trends/daily`、`/api/models`、`/api/meta`
  新增 `start` / `end` 绝对日期参数(ISO `YYYY-MM-DD`,**闭区间**,按本地日切),缺省 = 全量、
  行为与之前完全一致;`start > end` 返回 422。窗口在**三个适配器内部下推**(zcode 用
  `date(started_at/1000,…,'localtime')` 与日序列同一条表达式、opencode 用 `json_valid` 守护的
  `json_extract` 在 SQL 层过滤、minimax 在账本装载层按日剪枝),不在 API 层拿全量再筛——
  否则「某天未计价则该天成本为 null」的判定会被外层过滤破坏。相对窗口(「近 7 天」)由前端
  翻译成绝对日期,后端不猜锚点。

- 多来源支持:新增 MiniMax Code 与 opencode 数据源适配器;API 层 MultiSource 合并展示,\n  每个明细行带 `source` 标记,全部端点支持 `?source=` 过滤;前端表格与错误分布显示来源。\n- 六个数据视图全部升级为多来源合并视图(趋势序列按\"来源/模型\"区分)。\n- 全部六个数据视图:总览、日趋势(多序列折线 + 时间范围切换 + 每日/每周/累计活动图)、
  按模型(占比环形图 + 排行)、按项目、性能(P50/P90/P99)、健康度(重试/错误分布)。
- 前端壳与总览视图(React SPA:六视图导航、KPI 大数字卡片行、按模型明细表、
  未计价模型提示、数据源错误降级提示);生产构建由 FastAPI 托管为单进程,
  开发期 Vite 代理 `/api`。
- 项目骨架:uv 依赖管理 + ruff(E/F/I/UP)+ pytest(live marker 分层)+ pre-commit local hooks,
  治理模式沿用 deer-flow(AGENTS.md 单一事实源、文档随代码同步、push 前 make check)。
- v1 需求规格 `docs/specs/v1-spec.md` 与实施票 `docs/plans/tickets/`。
- 只读查询 API:`/api/meta`、`/api/overview`(含价格表成本估算)、`/api/trends/daily`、
  `/api/models`、`/api/projects`、`/api/performance`(P50/P90/P99)、`/api/health`。
- VLM 截图识别:设置页可配置视觉语言模型(OpenAI 兼容,密钥只存本机 gitignored 配置);
  价格表页按模型粘贴单价截图,识别结果只预填被选中的那一行,互不影响。
- 主题设置:设置页新增「主题」区块——深色/浅色外观切换 + 六种主题色(天蓝/青色/翡翠/
  靛紫/琥珀/玫红),选择立即生效并保存在本机浏览器(localStorage)。
- 美元价在录入期折算成人民币:价格表页顶部新增「1 美元 = ? 人民币」汇率(只用于录入期,
  不参与成本计算,刻意不内置默认值)。截图识别现在读取图上的币种标记——是 $ 就按汇率折成
  人民币再预填(提示里打出 `$3 → ¥21.6` 的逐项对照),汇率没填就拒绝预填并让你先填;图上没有
  任何币种标记就按人民币原样填,一个汇率也不乘。折算只此一处自动(「识别」列表头标着
  `($→¥)`),手工抄的美元价请自己换算后填数字,折算是一次性的:汇率之后改了不回溯已填的行。
- 买断（套餐）价与两个总额:价格表每行新增可填的「**买断价 ¥**」(为该渠道一次性付过的钱)与
  只读的「**现总价**」(按已保存单价 × 该渠道用量算出,与总览同源,不写回 `pricing.json`)。
  总览成本区改为并排两个数——「按量消耗」与「买断支出」,**永不相加**:一个是正在烧的钱
  (继续守「有渠道未计价则不显示总额」),一个是已经付掉的钱(不因缺单价而变未知)。买断价
  **留空与填 0 严格区分**(显示 `—` 与 `¥0.00`),也不摊成每百万等效单价——摊出来是假精确。
  动机是真实数据:15 行价格表里 10 行四档全 0、覆盖六成 token,一律显示 `¥0.00` 让「总成本」
  读起来像那些用量不要钱。见 `docs/specs/v2.4-prepricing-spec.md`。

### Changed

- 总览页按 Ardot 设计稿「AI用量总览-优化版」重排(T23,阶段 A 纯视觉):Header 标题行 →
  **Hero 主指标卡**(「按量消耗 (CNY)」大数字 + 估算口径文案,口径补上了此前漏掉的
  「缓存写」)→ 未计价渠道提醒条 → **5 张 KPI 卡**(总请求次数 / 累计 Token / 缓存读 /
  输出 / 实购支出,token 卡附未缩写原始数便于互相校对)→ 按模型明细表 → 页脚(数据更新
  时间 + 版本号)。配色全部走现有 zinc token,明暗两主题自动成立,light 模式下卡片实测
  `#f4f4f5`/`#e4e4e7` 与设计稿逐值一致;未计价模型在 Hero 显示「未计价」,环比位与缓存
  命中率在 T16/T21 落地前刻意留空、不渲染占位数字。`/api/meta` 新增 `version` 字段
  (取自 `zlens.__version__`),页脚版本号不再硬编码。
- 总览 KPI「估算成本」改名「按量消耗」:旁边多了「买断支出」,旧名字不再说清它量的是哪笔钱。
- 金额基准从美元改为人民币,并且**只存人民币**:总览 KPI、按模型 / 按项目成本列、价格表单位
  一律显示 ¥;界面上不再有币种标签或每行币种状态——一种币种、一种状态,不会看串行。
- **破坏性**:API 成本字段从 `estimated_cost_usd` 改名为中性的 `estimated_cost`(基准已是
  人民币,旧名会说谎;界面负责标币种);`pricing.json` 新增顶层 `fx_usd_cny`,
  **老条目一律按人民币读、不做迁移**——此前确按美元填的价请自己按汇率换算后重填。
- 价格表按渠道计价:`pricing.json` 的键从裸 `model_id` 改为渠道三元组
  `source|provider_id|model_id`(与别名键同款,由 `sources/models.py` 的 `model_key`
  统一生成),同一个模型经不同渠道提供时各填各的价,取价不再跨渠道合并;
  `/api/meta` 的 `unpriced_models`、总览未计价提示与价格表页同步改为一行一渠道
  (来源徽章 + 别名/模型名,渠道细节进悬停),`PUT /api/pricing` 对非三元组键返回 422。
  **破坏性**:早期以裸 `model_id` 为键的条目不再命中且不做迁移,价格表页会将其标为
  「旧格式键」并提供一键清除,请按渠道重填后保存。
- 渠道行只印一个名字:价格表把「来源 / 渠道名」拆成两列(与总览、按项目同构),
  来源徽章长短不再把名字起线推来推去;两列一起吸附在左侧,横滚时仍认得出在给谁定价。
  渠道号(UUID 尤其吵)完全退出行内文本,有别名显示别名、否则显示模型名,完整
  `source|provider_id|model_id` 键与模型/渠道信息进悬停。同名渠道的区分度交给
  总览「别名」列——用户起的名字才是认得出的区分,机器 id 不是。
- 价格表页移除「+ 新增模型」手工行:渠道行全部来自用量,手写键永远匹配不到任何行;
  截图识别仍只预填被武装的那一行,按该行自己的模型名匹配。
- 暗色主题层次重调:页面底提亮至 `#0f1215`、卡片改实色亮一级、边框与正文对比按
  主流暗色设计(GitHub / VS Code 式分层灰)调整,告别近纯黑一片糊。
- 日趋势图例移出图内画布,改为图下方的可点击标签行(点击隐藏/显示曲线),
  默认只启用用量 Top 8 模型、其余置灰折叠,不再遮挡网格线与刻度。
- 按模型环形图图例同样移出画布,改为图下方两栏信息列表(色点 + 模型 + 占比 +
  token 数),环形图居中,窄栏时不再与图重叠。
- 侧栏按目的聚合:8 项收为 5 项并加分组小标题(分析 / 配置)——总览、用量分析、
  运行质量、价格表、设置。日趋势 / 按模型 / 按项目合并为「用量分析」页内三 tab
  (子路由 `/usage/trends|models|projects` 可深链);性能 + 健康度合并为「运行质量」。
- 模型别名:总览明细表新增可编辑「别名」列(存本机浏览器,留空即用默认名),
  模型列保留原名作唯一标识;排行 / 环形图 / 日趋势按别名显示,悬停可看原名。
- 日趋势按 provider 拆分序列:同名不同路由的模型不再合并成一条线;视图内名字
  碰撞时追加 provider 短哈希后缀兜底区分。

### Fixed

- **买断支出「没填」与「填了 0」不再混同**(T14,存量正确性修复):此前价格表里一条买断行
  都没有时,总览 KPI 也显示 `¥0.00`,和真填了 0 的免费套餐长得一模一样——把「没填」画成
  ¥0.00 就是撒谎。现在 API 的 `buyout_total` 无任何买断行时返回 `null`,总览显示「未填」;
  只要有行填了金额(哪怕 0)就返回数值,`0.0` 显示「¥0.00 · 免费套餐」;多行求和照旧跳过
  空值,两笔钱照旧永不相加。
- 总览 KPI 条不再被窄栏压碎:此前 `flex-1` 允许每格窄于内容,而中文没有词边界,于是
  「5.48 亿」拆成两行、「省钱大户」一字一行、最后一格直接溢出卡片圆角。改成按**容器自身
  宽度**分三档:≥900px 一行六格 → 3+3 两行 → 2+2+2 三行(不用 `auto-fit`:六格挤不进时它会
  留下一个被拉宽的孤格,分档保证每行排满)。数值与提示一律 `nowrap`,分隔线由 1px 间隙露出
  底色画成,折几行都对。实测 1400/1100/901 → 6 列 1 行,899/700/561 → 3 列 2 行,
  559/420 → 2 列 3 行,八档宽度下裁字的格数为 0。
- **成本不再对缓存重复计价**(数字整体下调):ZCode 的 `input_tokens` 把命中的缓存前缀算在
  里头(真实库 2547/2547 行 `cache_read <= input`、2652/2652 行 `total = input + output`),
  而成本公式按四档各自乘价,于是同一批 token 先按全价输入档、再按缓存档各收一次。实测你已计价
  渠道的合计从 ¥444.45 降到 ¥99.98(高估 4.4×;单渠道最高 kimi-k3 ¥262.11 → ¥44.23)。
  现在契约是**四档互斥、相加即总计**:ZCode 适配器交出「未命中缓存的输入」(逐行相减并 clamp,
  一条脏数据不能把别行的输入减成负数),opencode 把 reasoning 并进输出档(它把 reasoning 当
  独立加数计入 total),MiniMax 本就互斥、零改动。副作用是你最初的疑问一并消失:四列相加现在
  正好等于「总计」。
- 「识别」列不再为最长文案留空地:点亮后的按钮原文案「已就绪：Ctrl/Cmd+V 粘贴」宽 160px，
  静止态只有 93px，`table-fixed` 逼着列宽按最长状态预留 176px——每行右侧常年挂着 83px 空白。
  现在点亮态换成更短的行动指令「Ctrl+V 粘贴」(86px，比静止态还短，按钮宽度从此不变)，
  列宽收到 112px，省出的 64px 分给四个单价列、横滚底线由 840 降到 776;macOS 的 ⌘V 写进悬停。
- 价格列表头与输入框右边界对齐:表头写着右对齐，实测却比它标注的输入框**缩进 6px**(缓存写缩进
  10px)。两个原因:输入框带 `min-w-14`,列宽收到 66px 底线时框会顶穿单元格自己的右内边距(与
  此前「表格顶穿容器」同一种毛病,只是缩进了单元格里);缓存写的表头用 `pr-5` 而它的格子用
  `pr-4`,又多错 4px。去掉框的最小宽、四列表头统一 `pr-4`,实测四列右边界误差 0px。
- 宽表格不再顶穿容器:此前 `w-full` 表格的最小宽由内容决定(价格表 4 个固定宽输入框
  最甚),栏宽一压就穿过卡片右边并顶出文档级横向滚动(右侧露出灰带)。现在总览/按模型
  明细表、价格表、按项目三处表格各自套横向滚动壳(`overflow-x-auto` + 表格 `min-w`
  底线),卡片宽度恒等于列宽;价格表另改 `table-fixed` + 列宽固定、价格输入框随列弹性
  收缩——先自己挤,挤到 776px 底线才在卡片内出滚动条,且「来源 / 渠道名」两列吸附在左侧,
  横滚时仍认得出在给哪一行定价。
- 总览「别名」列不再吞输入:此前输入框直接受控于规范化后的存储值(写入时 trim、
  与原名相同则丢弃),于是粘贴原名等于什么都没粘、输入 "kimi-k3-官方" 路过 "kimi-k3"
  的瞬间整框被清空。现在单元格持有本地草稿(所见即所输),只在失焦时与存储对齐一次
  ——对齐后仍会诚实回落到原名占位,不假装保存了无效别名。
- 图表配色跟随外观模式:此前 ECharts 硬编码暗色,浅色模式下热力图格子呈纯黑等
  错位问题,现深/浅各有一套调色板并随设置即时切换。
- 窄视口/缩放时页面出现横向滚动、右侧露出近黑色带:启动防闪色同步为新暗色底,
  页面背景改画在 html 上并跟随主题(溢出区同色),外壳消除横向溢出(main `min-w-0`、
  控制行可换行、图表容器裁剪),横向布局固定为视口宽度。
- VLM 配置保存后「测试连接 / 截图识别」立即生效(重新叠加本机配置,不再使用服务启动时快照)。
- 浏览器自动填充会把输入框(如 VLM API Key)涂成白色底,现与页面主题一致(深/浅色均保持)。
- 密钥安全:`zlens.config.json`(含 VLM Key)不再纳入版本库跟踪(gitignore)。
